export async function clusterStories() {
  // TODO: load recent articles with embeddings
  // TODO: cluster by similarity + recency window
  // TODO: upsert story clusters and story-article joins
  return {
    clustered: 0
  };
}
